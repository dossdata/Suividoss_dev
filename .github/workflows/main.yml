name: CD
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
      
      - name: APT UPDATE and INSTALL SSHPASS
        run: 
          sudo apt update
          
      - name: deploy
        run: 
          sshpass -f key.pem ssh -o PreferredAuthentications=privatekey bitnami@3.122.72.29
          "git pull"